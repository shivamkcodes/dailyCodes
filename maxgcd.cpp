// CPP program to illustrate
// gcd function of C++ STL
#include <iostream>
#include <algorithm>
#include <set>
#define in long long
using namespace std;

int main()
{
    // in t;
    // cin >> t;
    // for (int i = 0; i < t; i++)
    // {

    // in n;
    // cin >> n;
    // set<int> arr;
    cout << "gcd(6, 20) = " << __gcd(2, 4) << endl;

    // for (int i = 1; i <= n; i++)
    // {
    //     for (int j = i + 1; j <= n; j++)
    //     {
    //         if (i < j)
    //         {
    //             arr.insert(__gcd(i, j));
    //             /* code */
    //             // cout << i << "," << j << " ";
    //         }
    //     }
    // cout << endl;
    // }
    // cout << *arr.rbegin() << endl;
    // }
    // for (int x : arr)
    //     cout << x << " ";
    return 0;
}
