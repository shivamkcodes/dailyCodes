#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <set>
#include <stack>
#include <queue>
#define in long long
#include <string>
using namespace std;

int main()
{

    // int n;
    // cin >> n;
    vector<int> arr = {1, 2, 3, 4};
    // for (int &it : arr)
    //     cin >> it;

    int factor;
    cin >> factor;

    vector<pair<int, int>> ranges = {{0, 2}, {1, 3}, {2, 3}};

    // for (auto x : ranges)
    //     cout << x.first << "  " << x.second << endl;

    vector<int> v(5, 0);

    return 0;
}